.group
  =f.label :message
  =f.error_message_on :message
  ~f.text_area :message, :class => :text_area
  %span.description Ex: a simple text


.group.navform.wat-cf
  =f.submit pat(:save), :class => :button
  =f.submit pat(:cancel), :onclick => "window.location='#{url(:activities, :index)}';return false", :class => :button
